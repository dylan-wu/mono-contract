{
  "name": "license-logic-mono",
  "packageManager": "yarn@3.5.1",
  "private": true,
  "scripts": {
    "migrate:new": "sqlx migrate add -r",
    "migrate:up": "sqlx migrate run"
  },
  "workspaces": [
    "packages/*",
    "apps/frontend"
  ],
  "dependencies": {
    "d3": "^7.8.5"
  }
}
